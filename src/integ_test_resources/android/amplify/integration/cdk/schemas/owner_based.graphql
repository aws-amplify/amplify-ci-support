# Scenario A

type Post @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  title: String!
  owner: String
}

type Post @model
  @auth(rules: [{ allow: owner, operations: [create, delete] }]) {
  id: ID!
  title: String!
  owner: String
}